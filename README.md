# Movie Catalog

## Prerequisited
- Run the complement backend for the project

## Configuration
- Add env variable REACT_APP_HOST for the backend host

## Run
- Run `yarn install`
- Run `yarn start`
