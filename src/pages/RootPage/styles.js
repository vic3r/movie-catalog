import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
  root: {
    paddingTop: '15rem',
    fontFamily: 'Arial',
    color: '#3b5998',
    letterSpacing: 2,
  },
  button: {
    paddingTop: '0.5rem',
  },
}));

export default styles;
